# HORAI AI（宝来社 社内DXデモ）

社員が自然な文章をチャットに入力すると、AI（Claude）が内容を理解し、案件管理・車両管理・通知・売上集計などの社内システムへ振り分けるデモアプリです。

このプロジェクトは Next.js（App Router）で構築されており、Anthropic APIキーはサーバー側（API Route）でのみ扱われます。ブラウザ側のコードにAPIキーが含まれることはありません。

## 構成

```
利用者のブラウザ
  ↓ fetch("/api/ai")
Next.js API Route（app/api/ai/route.js）
  ↓ Anthropic公式SDK
Anthropic API（Claude）
  ↓ 解析結果（JSON）
Next.jsの画面（app/page.jsx）へ返す
```

## 1. ローカルで動かす

```bash
npm install
npm run dev
```

ブラウザで以下を開いてください。

```
http://localhost:3000
```

## 2. モックモード（APIキーなしで動作確認）

`ANTHROPIC_API_KEY` を設定していない状態で `npm run dev` を実行すると、自動的に「モックモード」で動作します。Anthropic APIは呼ばれず、サーバー側の簡易ルール（キーワード・日付表現のパターンマッチ）で依頼内容を解析します。

- 画面の一番上と設定画面に、モックモードで動作している旨が表示されます
- 「プリウスを明日から2日間予約して」のような典型的な言い回しは動作しますが、Claude本来の柔軟な自然文理解と比べると解析の精度は大きく劣ります
- APIキーを持っていない段階で、画面のデザインや操作フローだけを確認したいときに使ってください

APIキーを設定していても、常にモックモードで動かしたい場合（デモ中にAPI利用料をかけたくない場合など）は、`.env.local` に以下を追加してください。

```
MOCK_MODE=true
```

本来のAI理解を使う場合は、`ANTHROPIC_API_KEY` を設定し、`MOCK_MODE` は空欄または `false` にしてください。

## 3. プレゼンモード（社長プレゼンなど、確実に動かしたい本番用）

設定画面（もっと → 設定）に「プレゼンモード」というスイッチがあります。これをオンにすると：

- **外部のAI通信を一切行いません。** Anthropic APIはもちろん、自分のサーバー（`/api/ai`）へのリクエストすらも発生しません。すべてブラウザ内だけで依頼内容を解析します
- そのため、当日の会場のWi-Fi、Anthropic側の障害、APIの利用上限などに一切左右されず、確実に動作します
- モックモードのときに表示される「モックモードで動作中です」という注記バナーも、プレゼンモード中は表示されません（プレゼン中に「未設定」のような表示が出て気まずくなるのを防ぐためです）
- 文章の理解力は、モックモードと同じ簡易ルールベースです。本来のClaude接続ほど柔軟な言い回しには対応できないため、事前に用意されている例文（ホーム画面のサジェストチップに表示されるもの）を使うのが最も安全です

**社長プレゼンの前に**：設定画面でプレゼンモードをオンにしてから発表してください。設定はブラウザに保存されるため、一度オンにすれば次回開いたときもオンのままです。プレゼンが終わったら、通常のAI理解に戻すためにオフに切り替えてください。

## 4. `.env.local` の作り方

プロジェクト直下に `.env.local` というファイルを作成し、以下のように記入します（`.env.example` をコピーして使うと簡単です）。

```
ANTHROPIC_API_KEY=取得したAPIキー
ANTHROPIC_MODEL=利用するモデル名
```

- `ANTHROPIC_API_KEY` は [console.anthropic.com](https://console.anthropic.com) で発行したキーを入れてください。
- `ANTHROPIC_MODEL` は空欄のままでも動作します（未設定時はサーバー側のデフォルトモデルが使われます）。
- `.env.local` は **絶対にGitHubへアップロードしないでください**（`.gitignore` に既に登録済みです）。

## 5. Vercelへ公開する手順

1. このコードをGitHubリポジトリへアップロードする
2. [vercel.com](https://vercel.com) にログインする
3. 「Add New Project」から、アップロードしたGitHubリポジトリを選択する
4. プロジェクト設定の **Environment Variables** で以下を登録する
   - `ANTHROPIC_API_KEY`：取得したAPIキー
   - `ANTHROPIC_MODEL`：必要であれば指定（未設定でも可）
   - `MOCK_MODE`：常にモックモードで動かしたい場合のみ `true` を設定（通常は不要）
5. 「Deploy」を実行する
6. 環境変数を後から変更した場合は、再デプロイ（Redeploy）を行う

## 現在のデモ版でできること

- チャットに自然文を入力すると、Claudeが依頼内容を解析し、以下の業務に振り分けます
  - 車両予約（日付の重複チェック、空き車両の案内つき）
  - 新規案件の登録（案件名が読み取れない場合は聞き返します）
  - 見積期限が近い案件の一覧表示
  - 担当者への通知依頼（宛先・内容が不足している場合は聞き返します）
  - 今月の売上サマリー表示
- 入力データ（案件・車両予約・通知・チャット履歴）は、このブラウザの `localStorage` に自動保存されます。ページを再読み込みしても消えません。
- 設定画面の「プレゼンモード」をオンにすると、外部通信なしで確実に動作する状態に切り替えられます（社内発表など、通信環境に左右されたくない場面向け）。
- Google連携を設定すると、車両予約が自動でGoogleカレンダーに登録され、案件・予約・通知データをGoogleスプレッドシートへ書き出せます（任意設定・6章参照）。

## 現在はまだできないこと

- 複数の端末・利用者間でのアプリ内データ共有（`localStorage`はブラウザごとです。Google連携をオンにすれば、スプレッドシート経由で他の人にもデータを見せることはできます）
- 案件・車両・社員情報の手動編集・削除フォーム
- 売上データはサンプル固定値です（実データとの連携はまだありません）
- ユーザーごとのログイン・権限管理
- スプレッドシート側での編集内容をアプリに取り込む「読み込み」機能（現在は書き出しのみの一方向です）

## 6. Google連携（カレンダー・スプレッドシート）の設定手順

車両予約が成立すると自動でGoogleカレンダーに登録され、案件・予約・通知のデータをGoogleスプレッドシートへ書き出せます。どちらも任意機能で、設定しなければ今まで通りの動作（ブラウザ内保存のみ）になります。仕組みは「サービスアカウント」というGoogle側の自動化用アカウントを使い、あなた個人のログインを介さずにサーバー（Next.jsのAPI Route）だけがGoogleへアクセスします。

### 6-1. Google Cloudプロジェクトを作る

1. [console.cloud.google.com](https://console.cloud.google.com/) にアクセスし、Googleアカウントでログインする
2. 画面上部のプロジェクト選択から「新しいプロジェクト」を作成する（名前は「horai-ai」など何でも構いません）

### 6-2. 必要なAPIを有効化する

作成したプロジェクトを選択した状態で、以下2つのAPIをそれぞれ検索して「有効にする」を押します。

- **Google Calendar API**
- **Google Sheets API**

### 6-3. サービスアカウントを作る

1. 左側メニューから「APIとサービス」→「認証情報」を開く
2. 「認証情報を作成」→「サービスアカウント」を選ぶ
3. 名前を入力して作成する（例：`horai-ai-bot`）
4. 作成したサービスアカウントの一覧から、今作ったものをクリックする
5. 「キー」タブ →「鍵を追加」→「新しい鍵を作成」→ 形式は **JSON** を選んで作成する
6. JSONファイルがダウンロードされます。このファイルの中に以下の2つの値があります。
   - `client_email`：これが `GOOGLE_SERVICE_ACCOUNT_EMAIL` になります
   - `private_key`：これが `GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY` になります（`-----BEGIN PRIVATE KEY-----` から始まる長い文字列です）

このJSONファイルはパスワードと同じくらい重要な情報です。GitHubには絶対にアップロードしないでください。

### 6-4. Googleカレンダーと連携する

1. Googleカレンダーで、車両予約専用の新しいカレンダーを作成する（個人のメインカレンダーではなく、新規に作るのがおすすめです）
2. 作成したカレンダーの「設定」→「特定のユーザーとの共有」で、6-3で控えた `client_email`（サービスアカウントのメールアドレス）を追加し、権限は「予定の変更権限」にする
3. 同じ設定画面にある「カレンダーの統合」から「カレンダーID」をコピーする（`xxxxx@group.calendar.google.com` のような文字列）
4. これを `.env.local` の `GOOGLE_CALENDAR_ID` に設定する

### 6-5. Googleスプレッドシートと連携する

1. 新しいGoogleスプレッドシートを作成する
2. 右上の「共有」ボタンから、6-3で控えた `client_email` を「編集者」権限で追加する
3. スプレッドシートのURL（`https://docs.google.com/spreadsheets/d/【この部分】/edit`）の「この部分」をコピーする
4. これを `.env.local` の `GOOGLE_SHEETS_SPREADSHEET_ID` に設定する
5. シートのタブ（「案件」「車両予約」「通知」）は、初回の書き出し時にアプリが自動で作成します。手動で作る必要はありません

### 6-6. `.env.local` にまとめて設定する

```
GOOGLE_SERVICE_ACCOUNT_EMAIL=horai-ai-bot@your-project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_CALENDAR_ID=xxxxx@group.calendar.google.com
GOOGLE_SHEETS_SPREADSHEET_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

`GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY` は改行を含む長い文字列です。ダウンロードしたJSONファイルの `private_key` の値をそのままダブルクォートで囲んで貼り付ければ動作します。

設定後は `npm run dev` を再起動してください。設定画面（もっと → 設定）の「Google連携」欄が「連携済み」に変われば成功です。Vercelで使う場合は、同じ4つの値をVercelのEnvironment Variablesにも登録してください。

### 使い方

- **カレンダー**：設定不要。「ハイエースを明日から2日間借りたい」のように予約が成立すると、自動でカレンダーに登録されます
- **スプレッドシート**：設定画面から「今すぐ書き出す」を押すと、その時点のデータで3つのタブ（案件／車両予約／通知）を上書きします。「自動で書き出す」をオンにすると、データが変わるたびに自動で反映されます

### 制限事項

- スプレッドシートへの書き出しは、書き込みのたびに対象タブの内容を全て置き換える方式です（重複防止のため）。スプレッドシート側で手動編集した内容は、次の書き出しで上書きされます
- カレンダー登録に失敗しても、アプリ内の予約自体は成立したままになります（チャットにその旨のメッセージが表示されます）
- 個人のメインカレンダーに直接登録したい場合、サービスアカウント方式では権限の都合上うまくいかないことがあります。手順6-4のように専用カレンダーを新規作成し、それを共有する方法を推奨します

## 注意事項

- `.env.local` はGitHubへアップロードしないでください
- APIキーやGoogleサービスアカウントの秘密鍵を他人へ送らないでください
- 現在の保存先はブラウザの `localStorage` のため、端末間・利用者間では共有されません（Google連携を設定すれば、スプレッドシート経由で共有可能です）
